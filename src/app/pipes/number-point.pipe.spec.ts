import { NumberPointPipe } from './number-point.pipe';

describe('NumberPointPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPointPipe();
    expect(pipe).toBeTruthy();
  });
});
